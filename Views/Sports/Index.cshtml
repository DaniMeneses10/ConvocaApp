@model IEnumerable<ConvocaApp.Models.SportsModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.playerNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.accesories)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Id)
            </th*@>            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.playerNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.accesories)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>*@           
            <td>
                @Html.ActionLink("Edit", "Edit", new {item.Id /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { item.Id/* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { item.Id/* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
